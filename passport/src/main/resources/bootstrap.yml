server:
  port: 8080
  error:
    include-exception: true

spring:
  profiles:
    active: @profile.active@
  application:
    name: passport
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.6.106:8848
      config:
        server-addr: 192.168.6.106:8848
        file-extension: yml

feign:
  httpclient:
    enabled: true

logging:
  path: ./logs
  level:
    root: INFO
    com.atinbo: INFO
