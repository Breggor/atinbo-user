server:
  port: 9090

spring:
  profiles:
    active: @profile.active@
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.6.106:8848
      config:
        server-addr: 192.168.6.106:8848
        file-extension: yml

feign:
  httpclient:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  path: ./logs
  level:
    root: INFO
    com.atinbo: INFO